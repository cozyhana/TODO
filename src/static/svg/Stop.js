import React from "react";
import Svg, { Path } from "react-native-svg";
const Stop = props => (
  <Svg width={32} height={32} {...props}>
    <Path d="M26.114 16L5.887 32V0z" />
  </Svg>
);

export default Stop;
